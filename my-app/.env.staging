# ═══════════════════════════════════════════════════════════════
# BAITHAKA GHAR - STAGING ENVIRONMENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# This file contains staging environment variables
# Copy this to .env.local for staging deployment
# ═══════════════════════════════════════════════════════════════

# Environment
NODE_ENV=development
NEXT_PUBLIC_APP_ENV=staging
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Database (Use separate staging database)
MONGODB_URI=mongodb+srv://admin:YOUR_STAGING_PASSWORD@cluster0.jxpwth5.mongodb.net/baithaka-staging?retryWrites=true&w=majority

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-staging-secret-here-generate-with-openssl-rand-base64-32

# Razorpay (Use TEST MODE keys)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxx
RAZORPAY_KEY_SECRET=your_test_secret_here
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxx

# Email Configuration (Use test email service)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_mailtrap_user
SMTP_PASS=your_mailtrap_pass
SMTP_FROM=noreply-staging@baithakaghar.com

# Google OAuth (Optional for staging)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Cloudinary (Use separate staging cloud)
CLOUDINARY_CLOUD_NAME=your_staging_cloud
CLOUDINARY_API_KEY=your_staging_api_key
CLOUDINARY_API_SECRET=your_staging_api_secret
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_staging_cloud

# WhatsApp/SMS (Use test numbers)
TWILIO_ACCOUNT_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_token
TWILIO_PHONE_NUMBER=+1234567890

# Feature Flags
ENABLE_PARTIAL_PAYMENTS=true
ENABLE_OWNER_SYSTEM=true
ENABLE_PAYMENT_COLLECTION=true

# Logging
LOG_LEVEL=debug
ENABLE_REQUEST_LOGGING=true

# Testing
ENABLE_TEST_MODE=true
BYPASS_EMAIL_VERIFICATION=true

# ═══════════════════════════════════════════════════════════════
# NOTES:
# 1. Never commit this file to Git
# 2. Use different credentials than production
# 3. Use Razorpay TEST mode for all transactions
# 4. Use separate database for staging
# 5. All emails should go to test inboxes (Mailtrap)
# ═══════════════════════════════════════════════════════════════
